services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ToDoAppContainer
    ports:
      - '8080:8080'
    environment:
      - NODE_ENV=production
    volumes:
      - type: bind
        source: ./src
        target: /app/src
