{
  "name": "@todo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@12.5.1",
  "type": "module",
  "scripts": {
    "build:shared": "pnpm --filter shared run build",
    "build:frontend": "pnpm --filter frontend run build",
    "build": "pnpm run build:shared && pnpm run build:frontend"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
